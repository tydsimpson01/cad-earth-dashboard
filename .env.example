# Riot Games API - placeholders only; never commit real credentials.
RIOT_API_KEY=
RIOT_REGION=americas
RIOT_PLATFORM=na1

# Supabase public client configuration. Safe for browser use.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
